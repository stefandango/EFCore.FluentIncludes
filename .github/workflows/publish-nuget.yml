name: Publish to NuGet

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (leave empty to use version from csproj)'
        required: false
        type: string

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            8.0.x
            9.0.x
            10.0.x

      - name: Restore dependencies
        run: dotnet restore src/EFCore.FluentIncludes/EFCore.FluentIncludes.csproj

      - name: Build
        run: |
          dotnet build src/EFCore.FluentIncludes/EFCore.FluentIncludes.csproj -c Release -f net8.0 --no-restore
          dotnet build src/EFCore.FluentIncludes/EFCore.FluentIncludes.csproj -c Release -f net9.0 --no-restore
          dotnet build src/EFCore.FluentIncludes/EFCore.FluentIncludes.csproj -c Release -f net10.0 --no-restore

      - name: Pack
        run: |
          if [ -n "${{ github.event.inputs.version }}" ]; then
            dotnet pack src/EFCore.FluentIncludes/EFCore.FluentIncludes.csproj -c Release --no-build -p:Version=${{ github.event.inputs.version }}
          else
            dotnet pack src/EFCore.FluentIncludes/EFCore.FluentIncludes.csproj -c Release --no-build
          fi

      - name: Push to NuGet
        run: dotnet nuget push src/EFCore.FluentIncludes/bin/Release/*.nupkg --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
